
alias g="git status"
alias ga="git add ."
alias gc="git commit -m"
alias gphm="git push heroku master"
# alias git="hub" # if you have hub
alias gitc="git checkout"
alias gitcom="git add . && git commit -m "
alias assets="rake assets:precompile && rake assets:precompile"
